import clr
clr.AddReference("Microsoft.Office.Interop.Excel")
import Microsoft.Office.Interop.Excel as Excel
excel = Excel.ApplicationClass()
excel.Visible = True

workbook = excel.Workbooks.Add() #initializes workbook. (makes workbook visibile)
worksheet = workbook.Worksheets.Add() #adds a worksheet to the book. Not necessary because there's already 3


cell = worksheet.Range["A3"]#delcares range
cell.Value2 = 45 # if the sheet is visible, you'll see this appear.

print cell.Value2 #prints value of "cell" variable
print cell.Address[False, False] # prints A3 (the first two parameters are whether row and column should be absolute)

'''
from System.Reflection import Missing
# for optional parameters, like the arguments to the Address indexer, you can use Missing.Value to use the default.
print cell.Address[Missing.Value] # prints $A$3
print cell.Address[False, False] # prints A3 (the first two parameters are whether row and column should be absolute)

# or a number of cells (in which case .Value2 can be got/set as a 2D array).
xlrange = worksheet.Range["A3", "D4"]
print xlRange.Address[False, False] # prints A3:D4
print xlrange.Value2

from System import Array
a = Array.CreateInstance(object, 2, 2)
a[0, 1] = 3
a[1, 1] = "hi there!"

xlrange.Value2 = a # puts data in excel
print xlrange.Value2

cordinate system is (y, x)

'''

with open('mycsv.csv', 'w', newline='') as f:
     fieldnames = ['column1', 'column2', 'column3']
     thewriter = csv.DictWriter(f, fieldnames=fieldnames)
     thewriter.writeheader()
     for i in range(1, 10):
             thewriter.writerow({'column1':'one', 'column2':'two', 'column3':
'three'})


